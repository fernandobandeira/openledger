// Command openledger is the entrypoint for the ledger service.
//
// It does nothing yet. The first milestone is the ledger core (accounts,
// transactions, entries) and its invariants — see docs/roadmap.md.
package main

import "fmt"

func main() {
	fmt.Println("openledger: nothing to run yet — see docs/roadmap.md")
}
