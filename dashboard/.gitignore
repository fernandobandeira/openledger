# Dependencies
node_modules/

# Next.js build output
.next/
out/
next-env.d.ts

# Environment overrides — OPENLEDGER_API_ORIGIN belongs here, not in the repo
.env
.env.local

# Editor / OS noise
.DS_Store
*.tsbuildinfo

# NOTE: src/lib/api/ is NOT ignored. It is generated from
# crates/api/openapi.json and committed on purpose, and
# `npm run check:api-client` fails if what is committed is not what the spec
# generates — see README.md.
