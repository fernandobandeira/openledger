{
  "name": "openledger-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/api-reference.mjs && next dev",
    "build": "node scripts/api-reference.mjs && next build && pagefind --site out --output-subdir _pagefind",
    "start": "npx serve out"
  },
  "dependencies": {
    "beautiful-mermaid": "^1.1.3",
    "geist": "^1.7.2",
    "next": "^16.3.3",
    "nextra": "^4.6.1",
    "nextra-theme-docs": "^4.6.1",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "description": "A viewer over the repository’s own markdown. The docs live in ./content; this renders them.",
  "devDependencies": {
    "pagefind": "1.5.2",
    "redoc": "2.5.3"
  },
  "//redoc": "Exact-pinned: scripts/api-reference.mjs inlines its standalone bundle verbatim into public/api-reference/index.html, so the rendered API page changes only when this number does. 2.5.3 is the version spike 021 measured (1.10 MB, 3.5x smaller than Scalar for the same page).",
  "//zod": "nextra-theme-docs' Layout strips `children` from its props and then validates the remainder against a schema that still requires it. zod <4.4 lets a missing key through z.custom(); 4.4 made it 'expected nonoptional', which throws on every page. Held below 4.4 until nextra fixes the schema.",
  "overrides": {
    "zod": "<4.4"
  }
}
