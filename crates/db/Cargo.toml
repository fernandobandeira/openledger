# Connections and migrations: how a process reaches PostgreSQL, and how the
# schema gets there ahead of it. The migrations themselves stay at the
# repository root — they are adopter-facing files, not this crate's private
# assets; `build.rs` and the `sqlx::migrate!` path both point there.
[package]
name    = "db"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
sqlx.workspace  = true
tokio.workspace = true

[lints]
workspace = true
